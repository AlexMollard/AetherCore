# Per-project settings.
#
# Only app.startupScene is consumed at runtime — the engine loads window,
# graphics, and FPS settings from the shipped config://engine.toml and per-user
# overrides, NOT from this file.
#
# See resources/config/engine.toml for the shipped defaults.

[app]
startupScene = "default"
