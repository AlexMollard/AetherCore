# Testing Project

Project-root sample used to exercise the editor launcher, File Explorer, inspector drag/drop, scene save/load, and future publish flow.

Game assets live under `assets/`, scenes under `scenes/`, scripts under `scripts/`, and project settings under `settings/`.
