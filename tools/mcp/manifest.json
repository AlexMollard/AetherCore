{
  "methods": [
    {
      "description": "Create a sprite animation asset from atlas frames. 'frames' entries are sprite indices, sprite names, or {sprite, duration} objects; 'fps' sets the default frame duration and must be greater than 0. 'loop' is one of loop (restart, the default), once (stop on the last frame), pingpong (forwards then backwards) or hold (stop, keeping the last frame shown). Assign the result to a Sprite Animator component to play it.",
      "mutates": true,
      "name": "animation.create",
      "paramsSchema": {
        "properties": {
          "atlas": {
            "type": "string"
          },
          "fps": {
            "type": "number"
          },
          "frames": {
            "maxItems": 1024,
            "minItems": 1,
            "type": "array"
          },
          "loop": {
            "enum": [
              "loop",
              "once",
              "pingpong",
              "hold"
            ],
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "out": {
            "type": "string"
          }
        },
        "required": [
          "atlas",
          "name",
          "frames"
        ],
        "type": "object"
      },
      "tool": "create_sprite_animation"
    },
    {
      "description": "Select a file as the current ASSET selection, exactly as clicking it in the File Explorer does: the Material, Texture and Inspector windows all follow it. Path is a project-relative or VFS path.",
      "mutates": true,
      "name": "editor.select_asset",
      "paramsSchema": {
        "properties": {
          "path": {
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      },
      "tool": "select_asset"
    },
    {
      "description": "Glob project files by virtual path, e.g. 'project://assets/**/*.png' or 'project://assets/**/*.spriteatlas.toml'. Feeds texture/atlas parameters on the other 2D authoring methods. 'limit' caps how many paths come back while 'matched' and 'truncated' still describe the full result, so a small limit is enough to count what a glob hits.",
      "mutates": false,
      "name": "assets.list",
      "paramsSchema": {
        "properties": {
          "glob": {
            "type": "string"
          },
          "limit": {
            "type": "integer"
          }
        },
        "type": "object"
      },
      "tool": "list_assets"
    },
    {
      "description": "List a sprite atlas' regions: index, name, and pixel rect. Indices/names feed create_sprite_animation; names identify sprites for Sprite Renderer components. Returns spriteCount and the first 2,000 regions - check 'truncated'.",
      "mutates": false,
      "name": "atlas.info",
      "paramsSchema": {
        "properties": {
          "path": {
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      },
      "tool": "get_atlas"
    },
    {
      "description": "Grid-slice a texture into a sprite atlas asset (the same operation as the Sprite Slicer panel). Re-slicing an existing atlas keeps stable sprite ids where regions still correspond. cellWidth/cellHeight are pixels and must be at least 1. Returns the atlas path, spriteCount, and the first 2,000 sprites - check 'truncated', because the atlas on disk holds them all.",
      "mutates": true,
      "name": "atlas.slice",
      "paramsSchema": {
        "properties": {
          "cellHeight": {
            "type": "integer"
          },
          "cellWidth": {
            "type": "integer"
          },
          "out": {
            "type": "string"
          },
          "paddingX": {
            "type": "integer"
          },
          "paddingY": {
            "type": "integer"
          },
          "pixelsPerUnit": {
            "type": "number"
          },
          "spacingX": {
            "type": "integer"
          },
          "spacingY": {
            "type": "integer"
          },
          "texture": {
            "type": "string"
          },
          "trimAlpha": {
            "type": "boolean"
          }
        },
        "required": [
          "texture",
          "cellWidth",
          "cellHeight"
        ],
        "type": "object"
      },
      "tool": "slice_atlas"
    },
    {
      "description": "Active camera: projection mode, world position, forward direction, vertical field of view, and orthographic height.",
      "mutates": false,
      "name": "camera.info",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "camera_info"
    },
    {
      "description": "Read a bounded tail of the live editor console. Filter by minimumLevel (verbose/info/warn/error), category, message text, or monotonic afterSeq for incremental polling. 'limit' caps how many entries come back (1-500, default 100) while 'matched' still reports how many the filter actually hit, so counting errors costs one small call rather than the whole tail.",
      "mutates": false,
      "name": "console.logs",
      "paramsSchema": {
        "properties": {
          "afterSeq": {
            "minimum": 0,
            "type": "integer"
          },
          "category": {
            "type": "string"
          },
          "contains": {
            "type": "string"
          },
          "limit": {
            "maximum": 500,
            "minimum": 1,
            "type": "integer"
          },
          "minimumLevel": {
            "enum": [
              "verbose",
              "info",
              "warn",
              "error"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "tool": "get_console_log"
    },
    {
      "description": "Redo the last undone scene edit (mirrors Ctrl+Y). Returns ok=false when the redo stack is empty.",
      "mutates": true,
      "name": "edit.redo",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "redo"
    },
    {
      "description": "Undo the last scene edit (mirrors Ctrl+Z). Applies the top command in place so entity ids and the selection are preserved. Returns ok=false when the undo history is empty; undoDepth/redoDepth report the remaining stack sizes.",
      "mutates": true,
      "name": "edit.undo",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "undo"
    },
    {
      "description": "Move the edit-mode viewport camera: 'position' ([x, y] keeps z, [x, y, z] sets it), 'height' sets the orthographic view height, and 'frame' centres on an entity. Call it with no arguments to read the camera without moving it - it returns the current position and orthographic height either way. 'height' only lands in a 2D scene; a 3D edit camera is perspective and keeps the value it had. Edit mode only - the play camera belongs to the game.",
      "mutates": true,
      "name": "editor.camera",
      "paramsSchema": {
        "properties": {
          "frame": {
            "description": "entity id to centre on",
            "type": "integer"
          },
          "height": {
            "type": "number"
          },
          "position": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 2,
            "type": "array"
          }
        },
        "type": "object"
      },
      "tool": "set_editor_camera"
    },
    {
      "description": "Open the Inspector and scroll a specific component's drawer into view (force-opening it), e.g. 'Rigid Body' or 'Material'. Select an entity first. Matches the section label case-insensitively - use it to frame a component for a screenshot.",
      "mutates": true,
      "name": "editor.inspect_component",
      "paramsSchema": {
        "properties": {
          "component": {
            "type": "string"
          }
        },
        "required": [
          "component"
        ],
        "type": "object"
      },
      "tool": "inspect_component"
    },
    {
      "description": "Apply a built-in workflow dock layout by name (from list_layouts; case-insensitive): rebuilds the dockspace and shows that workflow's panels.",
      "mutates": true,
      "name": "editor.layout_apply",
      "paramsSchema": {
        "properties": {
          "name": {
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      },
      "tool": "apply_layout"
    },
    {
      "description": "List the built-in workflow dock layouts (e.g. '2D / Sprites', 'Rendering / Look-dev'). Names feed apply_layout.",
      "mutates": false,
      "name": "editor.layouts_list",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "list_layouts"
    },
    {
      "description": "Open the editor's Open Scene dialog (the same one as File > Open).",
      "mutates": true,
      "name": "editor.open_scene_dialog",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "open_scene_dialog"
    },
    {
      "description": "Open or close an editor panel by name (from list_windows; case-insensitive), e.g. show the Inspector so a screenshot captures it. Pass focus to also bring it to the front of its dock node - a panel sharing a node with others is visible while its tab is behind theirs, which reads as a panel that draws nothing.",
      "mutates": true,
      "name": "editor.window_set",
      "paramsSchema": {
        "properties": {
          "focus": {
            "type": "boolean"
          },
          "name": {
            "type": "string"
          },
          "visible": {
            "type": "boolean"
          }
        },
        "required": [
          "name",
          "visible"
        ],
        "type": "object"
      },
      "tool": "set_window"
    },
    {
      "description": "List every editor panel/window and whether it is currently open. Names feed set_window.",
      "mutates": false,
      "name": "editor.windows",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "list_windows"
    },
    {
      "description": "Freeze the running simulation (state='paused'). Session stays live; no-op unless playing.",
      "mutates": true,
      "name": "engine.pause",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "pause"
    },
    {
      "description": "Enter Play mode (async): rebuild the C# scripts on a worker thread, then snapshot + start. Returns immediately with state='compiling'; poll info.playState until 'playing'. The editor never blocks. Mirrors the Play button.",
      "mutates": true,
      "name": "engine.play",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "play"
    },
    {
      "description": "Play a timed input sequence for auto-testing (frame-accurate, driven on the game thread). Provide 'text' (inline) or 'file' (path to a .seq file). Each line is '<time_seconds> <op> [keys...]' where op is hold (keys stay down until a later release/clear), press/tap (a real key edge every time: down now, auto-released ~0.1s later - use this for repeated presses in one sequence), release/up (release keys; 'release all' clears everything), or clear; keys use the same names as send_input. '#' starts a comment. Keys auto-release when the sequence ends. Pass {stop:true} to abort a running sequence. Returns {events, duration}.",
      "mutates": true,
      "name": "engine.play_input_sequence",
      "paramsSchema": {
        "properties": {
          "file": {
            "type": "string"
          },
          "stop": {
            "type": "boolean"
          },
          "text": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "tool": "play_input_sequence"
    },
    {
      "description": "Unfreeze a paused simulation (state='playing'). No-op unless playing.",
      "mutates": true,
      "name": "engine.resume",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "resume"
    },
    {
      "description": "Inject synthetic keyboard state for headless playtesting: {down:[names], up:[names], clear?:bool}. Keys stay held until released, `clear`, or Stop. Names: left/right/up/down, space, enter, escape, tab, shift, ctrl, alt, backspace, delete, home, end, pageup, pagedown, f1-f12, or a single letter a-z / digit 0-9. OR'd over the real keyboard, so IsKeyDown and the IsKeyPressed down-edge both fire. Pass {text:\"...\"} to type characters into a focused text field. GAMEPAD: {pad_connected:true} presents a synthetic controller (slot via {pad_slot}, default 0); {pad_down:[names], pad_up:[names]} with a/b/x/y, lb/rb, lt/rt, back/start/guide, lthumb/rthumb, dpad_up/dpad_down/dpad_left/dpad_right; {pad_axis:{left_x:0.5, left_y:-1.0}} sets axes with names left_x/left_y/right_x/right_y/left_trigger/right_trigger. Axis values use the RAW controller convention (-1 is stick UP, and a released trigger is -1, not 0) so the engine's own normalisation is what gets exercised rather than bypassed.",
      "mutates": true,
      "name": "engine.send_input",
      "paramsSchema": {
        "properties": {
          "clear": {
            "type": "boolean"
          },
          "down": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "mouse_down": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "mouse_pos": {
            "items": {
              "type": "number"
            },
            "type": "array"
          },
          "mouse_up": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "mouse_world": {
            "items": {
              "type": "number"
            },
            "type": "array"
          },
          "pad_axis": {
            "type": "object"
          },
          "pad_connected": {
            "type": "boolean"
          },
          "pad_down": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "pad_slot": {
            "type": "integer"
          },
          "pad_up": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "text": {
            "type": "string"
          },
          "up": {
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "tool": "send_input"
    },
    {
      "description": "Set the play-speed multiplier (0.05-16; 1=normal, <1 slow-mo, >1 fast-forward). Persists across Play sessions, and 'info' reports the current value as 'speed'.",
      "mutates": true,
      "name": "engine.set_speed",
      "paramsSchema": {
        "properties": {
          "speed": {
            "maximum": 16.0,
            "minimum": 0.05,
            "type": "number"
          }
        },
        "required": [
          "speed"
        ],
        "type": "object"
      },
      "tool": "set_speed"
    },
    {
      "description": "Advance the simulation exactly {frames} frames (default 1; pauses first if running). Use for frame-by-frame debugging, and to reach a KNOWN frame before render.capture_texture - a paused frame renders bit-identically, so two runs stopped on the same frame can be compared pixel for pixel.",
      "mutates": true,
      "name": "engine.step",
      "paramsSchema": {
        "properties": {
          "frames": {
            "maximum": 100000,
            "minimum": 1,
            "type": "integer"
          }
        },
        "type": "object"
      },
      "tool": "step"
    },
    {
      "description": "Exit Play mode and restore the pre-play scene snapshot.",
      "mutates": true,
      "name": "engine.stop",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "stop"
    },
    {
      "description": "Toggle Play/Stop.",
      "mutates": true,
      "name": "engine.toggle_play",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "toggle_play"
    },
    {
      "description": "Live editor summary: current scene name and kind, entity count, frame index, fps, and any connected gamepads with their live stick/trigger/button state - the first thing to check when a controller seems dead.",
      "mutates": false,
      "name": "info",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "engine_info"
    },
    {
      "description": "Create a Blank 2D or Blank 3D project, then hand the control port from the Launcher to its Editor process.",
      "mutates": true,
      "name": "launcher.create_project",
      "paramsSchema": {
        "properties": {
          "name": {
            "type": "string"
          },
          "root": {
            "type": "string"
          },
          "template": {
            "enum": [
              "blank_3d",
              "blank_2d"
            ],
            "type": "string"
          }
        },
        "required": [
          "root",
          "template"
        ],
        "type": "object"
      },
      "tool": "create_project"
    },
    {
      "description": "Live Launcher summary: recent-project count, handoff state, frame index, and fps.",
      "mutates": false,
      "name": "launcher.info",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "launcher_info"
    },
    {
      "description": "Open an existing AetherCore project and hand the control port from the Launcher to its Editor process.",
      "mutates": true,
      "name": "launcher.open_project",
      "paramsSchema": {
        "properties": {
          "root": {
            "type": "string"
          }
        },
        "required": [
          "root"
        ],
        "type": "object"
      },
      "tool": "open_project"
    },
    {
      "description": "List the Launcher's recent projects, including each project name and root path.",
      "mutates": false,
      "name": "launcher.projects",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "list_projects"
    },
    {
      "description": "Re-point a moved project in the recent list to a new folder (headless: no folder picker).",
      "mutates": true,
      "name": "launcher.relocate_recent",
      "paramsSchema": {
        "properties": {
          "new_root": {
            "type": "string"
          },
          "old_root": {
            "type": "string"
          }
        },
        "required": [
          "old_root",
          "new_root"
        ],
        "type": "object"
      },
      "tool": "relocate_recent"
    },
    {
      "description": "Remove a project from the Launcher's recent list (works whether the project still exists or is missing).",
      "mutates": true,
      "name": "launcher.remove_recent",
      "paramsSchema": {
        "properties": {
          "root": {
            "type": "string"
          }
        },
        "required": [
          "root"
        ],
        "type": "object"
      },
      "tool": "remove_recent"
    },
    {
      "description": "Open a recent project's folder in the OS file manager.",
      "mutates": false,
      "name": "launcher.reveal_folder",
      "paramsSchema": {
        "properties": {
          "root": {
            "type": "string"
          }
        },
        "required": [
          "root"
        ],
        "type": "object"
      },
      "tool": "reveal_folder"
    },
    {
      "description": "Flood-fill the connected region at {x, y} with 'color' (or the active colour). One undo step.",
      "mutates": true,
      "name": "pixel.bucket",
      "paramsSchema": {
        "properties": {
          "color": {
            "description": "[r, g, b] or [r, g, b, a], 0-255; omit to use the current colour",
            "items": {
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            },
            "maxItems": 4,
            "minItems": 3,
            "type": "array"
          },
          "x": {
            "type": "integer"
          },
          "y": {
            "type": "integer"
          }
        },
        "required": [
          "x",
          "y"
        ],
        "type": "object"
      },
      "tool": "pixel_bucket"
    },
    {
      "description": "Clear the whole canvas to 'color' (default transparent). One undo step.",
      "mutates": true,
      "name": "pixel.clear",
      "paramsSchema": {
        "properties": {
          "color": {
            "description": "[r, g, b] or [r, g, b, a], 0-255; omit to use the current colour",
            "items": {
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            },
            "maxItems": 4,
            "minItems": 3,
            "type": "array"
          }
        },
        "type": "object"
      },
      "tool": "pixel_clear"
    },
    {
      "description": "Fill an inclusive cell rectangle {x0,y0,x1,y1, color?}. One undo step.",
      "mutates": true,
      "name": "pixel.fill_rect",
      "paramsSchema": {
        "properties": {
          "color": {
            "description": "[r, g, b] or [r, g, b, a], 0-255; omit to use the current colour",
            "items": {
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            },
            "maxItems": 4,
            "minItems": 3,
            "type": "array"
          },
          "x0": {
            "type": "integer"
          },
          "x1": {
            "type": "integer"
          },
          "y0": {
            "type": "integer"
          },
          "y1": {
            "type": "integer"
          }
        },
        "required": [
          "x0",
          "y0",
          "x1",
          "y1"
        ],
        "type": "object"
      },
      "tool": "pixel_fill_rect"
    },
    {
      "description": "Read the canvas back: dimensions plus a row-major 'pixels' array of 0xAABBGGRR values (only when <= 4096 texels; otherwise omitted).",
      "mutates": false,
      "name": "pixel.get",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "pixel_get"
    },
    {
      "description": "Current pixel-art canvas dimensions, dirty flag, and file path.",
      "mutates": false,
      "name": "pixel.info",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "pixel_info"
    },
    {
      "description": "Draw a line between two texels {x0,y0,x1,y1, color?}. One undo step.",
      "mutates": true,
      "name": "pixel.line",
      "paramsSchema": {
        "properties": {
          "color": {
            "description": "[r, g, b] or [r, g, b, a], 0-255; omit to use the current colour",
            "items": {
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            },
            "maxItems": 4,
            "minItems": 3,
            "type": "array"
          },
          "x0": {
            "type": "integer"
          },
          "x1": {
            "type": "integer"
          },
          "y0": {
            "type": "integer"
          },
          "y1": {
            "type": "integer"
          }
        },
        "required": [
          "x0",
          "y0",
          "x1",
          "y1"
        ],
        "type": "object"
      },
      "tool": "pixel_line"
    },
    {
      "description": "Create a fresh transparent pixel-art canvas of {width, height} (1-512). Clears undo history. Mirrors the Pixel Art panel's New.",
      "mutates": true,
      "name": "pixel.new",
      "paramsSchema": {
        "properties": {
          "height": {
            "type": "integer"
          },
          "width": {
            "type": "integer"
          }
        },
        "required": [
          "width",
          "height"
        ],
        "type": "object"
      },
      "tool": "pixel_new"
    },
    {
      "description": "Load a PNG at 'path' (project:// virtual path) into the canvas for editing.",
      "mutates": true,
      "name": "pixel.open",
      "paramsSchema": {
        "properties": {
          "path": {
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      },
      "tool": "pixel_open"
    },
    {
      "description": "Save the canvas to a PNG at 'path' (project:// virtual path; default: the current file). Creates folders.",
      "mutates": true,
      "name": "pixel.save",
      "paramsSchema": {
        "properties": {
          "path": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "tool": "pixel_save"
    },
    {
      "description": "Set individual texels: {pixels:[{x,y,color?}]}. 'color' omitted uses the active colour; use [0,0,0,0] to erase. One undo step for the batch.",
      "mutates": true,
      "name": "pixel.set",
      "paramsSchema": {
        "properties": {
          "pixels": {
            "items": {
              "properties": {
                "color": {
                  "description": "[r, g, b] or [r, g, b, a], 0-255; omit to use the current colour",
                  "items": {
                    "maximum": 255,
                    "minimum": 0,
                    "type": "integer"
                  },
                  "maxItems": 4,
                  "minItems": 3,
                  "type": "array"
                },
                "x": {
                  "type": "integer"
                },
                "y": {
                  "type": "integer"
                }
              },
              "required": [
                "x",
                "y"
              ],
              "type": "object"
            },
            "type": "array"
          }
        },
        "required": [
          "pixels"
        ],
        "type": "object"
      },
      "tool": "pixel_set"
    },
    {
      "description": "Set the active drawing colour {color:[r,g,b,a]}.",
      "mutates": true,
      "name": "pixel.set_color",
      "paramsSchema": {
        "properties": {
          "color": {
            "description": "[r, g, b] or [r, g, b, a], 0-255; omit to use the current colour",
            "items": {
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            },
            "maxItems": 4,
            "minItems": 3,
            "type": "array"
          }
        },
        "required": [
          "color"
        ],
        "type": "object"
      },
      "tool": "pixel_set_color"
    },
    {
      "description": "Publish the open project to a standalone package (Builds/<platform>/<product>). Runs asynchronously; poll publish_status for progress and the result. Uses the running editor's build configuration.",
      "mutates": true,
      "name": "project.publish",
      "paramsSchema": {
        "properties": null,
        "type": "object"
      },
      "tool": "publish_project"
    },
    {
      "description": "Progress and result of the publish started by publish_project: state is idle, running, succeeded or failed.",
      "mutates": false,
      "name": "project.publish_status",
      "paramsSchema": {
        "properties": null,
        "type": "object"
      },
      "tool": "publish_status"
    },
    {
      "description": "Per-pass GPU and CPU timing for the last rendered frame, sorted by GPU time (slowest first), plus the hottest pass, graph totals for both, and this frame's fps. GPU time is the one that answers \"what does this pass cost\" - CPU time here is only the cost of RECORDING the pass, so a heavy shader shows as expensive on gpuTimeMs and near-free on cpuTimeMs. Poll repeatedly to sample min/avg/max; single frames vary by ~10%.",
      "mutates": false,
      "name": "render.benchmark",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "render_benchmark"
    },
    {
      "description": "Capture a registered texture / render target (name from list_textures) to a .png and return its path - use it to SEE any GPU texture, not just the viewport. Handles 8-bit color, depth (normalized grayscale) and HDR (tonemapped) targets.",
      "mutates": false,
      "name": "render.capture_texture",
      "paramsSchema": {
        "properties": {
          "name": {
            "type": "string"
          },
          "path": {
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      },
      "tool": "capture_texture"
    },
    {
      "description": "GPU memory breakdown: what the driver says this process holds, what the allocator holds for it, and every texture / buffer that makes it up (largest first). Use it to find out where VRAM actually goes. 'limit' caps how many texture and buffer rows come back (default 24); pass 0 for the totals and heap figures with no rows, which is the whole answer when you only want to compare counts or watch for a leak.",
      "mutates": false,
      "name": "render.memory",
      "paramsSchema": {
        "properties": {
          "limit": {
            "type": "integer"
          }
        },
        "type": "object"
      },
      "tool": "render_memory"
    },
    {
      "description": "Render-graph frame statistics: pass/barrier counts, transient-image cache hit/miss, and the transient GPU heap capacity vs. bytes used - the engine's per-frame allocation profile.",
      "mutates": false,
      "name": "render.stats",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "render_stats"
    },
    {
      "description": "List every registered texture / render target (shadow atlas, GBuffer, scene color, asset textures): name, format, extent, aspect, mips, layers, bindless slot.",
      "mutates": false,
      "name": "render.textures",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "list_textures"
    },
    {
      "description": "Dump the live render graph: every compiled pass with its type, dependencies, produced/consumed frame products, and last CPU time.",
      "mutates": false,
      "name": "rendergraph",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "query_rendergraph"
    },
    {
      "description": "Add a component to an entity by id. 'type' is a ComponentCatalog name (call list_component_types for the set).",
      "mutates": true,
      "name": "scene.add_component",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          },
          "type": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "type"
        ],
        "type": "object"
      },
      "tool": "add_component"
    },
    {
      "description": "Add components to up to 10,000 entities in one request. Each item may also provide reflected field values, combining add and initialize without another round trip.",
      "mutates": true,
      "name": "scene.add_component_many",
      "paramsSchema": {
        "properties": {
          "items": {
            "items": {
              "properties": {
                "id": {
                  "type": "integer"
                },
                "type": {
                  "type": "string"
                },
                "values": {
                  "type": "object"
                }
              },
              "required": [
                "id",
                "type"
              ],
              "type": "object"
            },
            "maxItems": 10000,
            "minItems": 1,
            "type": "array"
          }
        },
        "required": [
          "items"
        ],
        "type": "object"
      },
      "tool": "add_components"
    },
    {
      "description": "Add a model file to the live scene as one operation - the seamless equivalent of dragging a glTF into the editor. Bakes the raw glTF if it hasn't been imported yet, then spawns the correct layout: a lone static mesh sits on a single entity, while a multi-primitive or skinned model becomes a root with one child per primitive (each carrying its own material and skin). 'path' is a project:// or absolute model path; optional 'name', 'position', 'rotationEuler' (degrees) and 'scale' place it (skinned models authored in centimetres usually want scale ~0.01). Returns the root id and primitive count.",
      "mutates": true,
      "name": "scene.add_model",
      "paramsSchema": {
        "properties": {
          "name": {
            "type": "string"
          },
          "path": {
            "type": "string"
          },
          "position": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "rotationEuler": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "scale": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      },
      "tool": "add_model"
    },
    {
      "description": "Place a LINKED prefab instance in the live scene: the scene stores a reference (not a copy), so editing the prefab propagates to every instance. 'prefab' is the prefab's save name; optional 'position'/'rotationEuler'/'scale' place the instance root. Returns the root id.",
      "mutates": true,
      "name": "scene.add_prefab_instance",
      "paramsSchema": {
        "properties": {
          "position": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "prefab": {
            "type": "string"
          },
          "rotationEuler": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "scale": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          }
        },
        "required": [
          "prefab"
        ],
        "type": "object"
      },
      "tool": "add_prefab_instance"
    },
    {
      "description": "Attach a C# script to an entity by type name (the class name in the project's scripts folder, e.g. 'PlayerController'). Optional 'properties' seeds public fields: numbers, bools, strings, or [x, y, z] arrays.",
      "mutates": true,
      "name": "scene.add_script",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          },
          "properties": {
            "type": "object"
          },
          "type": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "type"
        ],
        "type": "object"
      },
      "tool": "add_script"
    },
    {
      "description": "Apply a prefab instance's current state (its overrides included) back to the prefab file. Every other live instance is rebuilt immediately so the change shows at once, each keeping its own overrides; 'refreshed' returns their new ids (rebuilding changes them). 'id' is the instance root.",
      "mutates": true,
      "name": "scene.apply_prefab_instance",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "tool": "apply_prefab_instance"
    },
    {
      "description": "List every component (name + category), plus, for components with editable fields, the field list get_component / set_component accept.",
      "mutates": false,
      "name": "scene.component_types",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "list_component_types"
    },
    {
      "description": "Create an entity in the live scene. Returns its id.",
      "mutates": true,
      "name": "scene.create",
      "paramsSchema": {
        "properties": {
          "name": {
            "type": "string"
          },
          "position": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "rotationEuler": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "scale": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          }
        },
        "type": "object"
      },
      "tool": "create_entity"
    },
    {
      "description": "Create up to 10,000 entities in one request. Each item accepts name/transform plus optional components with initial reflected values. Processing is one editor-frame command; failures are reported per item and a failed item's partial entity is rolled back.",
      "mutates": true,
      "name": "scene.create_many",
      "paramsSchema": {
        "properties": {
          "items": {
            "items": {
              "properties": {
                "components": {
                  "items": {
                    "properties": {
                      "type": {
                        "type": "string"
                      },
                      "values": {
                        "type": "object"
                      }
                    },
                    "required": [
                      "type"
                    ],
                    "type": "object"
                  },
                  "maxItems": 64,
                  "type": "array"
                },
                "name": {
                  "type": "string"
                },
                "position": {
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "rotationEuler": {
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "scale": {
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                }
              },
              "type": "object"
            },
            "maxItems": 10000,
            "minItems": 1,
            "type": "array"
          }
        },
        "required": [
          "items"
        ],
        "type": "object"
      },
      "tool": "create_entities"
    },
    {
      "description": "Delete an entity from the live scene by id.",
      "mutates": true,
      "name": "scene.delete",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "tool": "delete_entity"
    },
    {
      "description": "Delete up to 10,000 entities in one request. Returns ordered per-item results with partial failures.",
      "mutates": true,
      "name": "scene.delete_many",
      "paramsSchema": {
        "properties": {
          "items": {
            "items": {
              "properties": {
                "id": {
                  "type": "integer"
                }
              },
              "required": [
                "id"
              ],
              "type": "object"
            },
            "maxItems": 10000,
            "minItems": 1,
            "type": "array"
          }
        },
        "required": [
          "items"
        ],
        "type": "object"
      },
      "tool": "delete_entities"
    },
    {
      "description": "List every entity in the live scene with id, name, world position, and parent id (0 when it sits at the root). The parent is what makes the list a tree rather than a flat set - without it nothing outside the editor can tell how the scene is nested.",
      "mutates": false,
      "name": "scene.entities",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "list_entities"
    },
    {
      "description": "Full detail for one entity by id: name, world position, rotationEuler and scale (the same three channels scene.transform writes), its component type list, and 'editable' - the subset of those names get_component and set_component accept. Unreflected internals appear in 'components' under their raw C++ type name; they are real, just not addressable.",
      "mutates": false,
      "name": "scene.get",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "tool": "get_entity"
    },
    {
      "description": "Read a component's editable fields - the same fields the Inspector shows - as a JSON object. 'type' is a component name (call list_component_types for the set and each type's fields). Returns {id, type, fields:{...}}, or an error if the entity lacks that component.",
      "mutates": false,
      "name": "scene.get_component",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          },
          "type": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "type"
        ],
        "type": "object"
      },
      "tool": "get_component"
    },
    {
      "description": "Every light in the live scene: id, name, type (point/spot), world position, color, intensity, radius, shadow-casting flag (spots also report cone angles and aim direction).",
      "mutates": false,
      "name": "scene.lights",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "list_lights"
    },
    {
      "description": "Switch the live editor to a scene by name (tears the current scene down first).",
      "mutates": true,
      "name": "scene.load",
      "paramsSchema": {
        "properties": {
          "name": {
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      },
      "tool": "load_scene"
    },
    {
      "description": "Replace the live scene with a fresh scene from the shipped default template. 'kind' picks the domain: '3d' (default) or '2d' (orthographic camera, sprite/tilemap/2D-physics features).",
      "mutates": true,
      "name": "scene.new",
      "paramsSchema": {
        "properties": {
          "kind": {
            "enum": [
              "2d",
              "3d"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "tool": "new_scene"
    },
    {
      "description": "Set an entity's parent by id. parent=0 unparents it to the scene root.",
      "mutates": true,
      "name": "scene.parent",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          },
          "parent": {
            "type": "integer"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "tool": "parent_entity"
    },
    {
      "description": "Remove a component from an entity by id ('type' is a ComponentCatalog name).",
      "mutates": true,
      "name": "scene.remove_component",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          },
          "type": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "type"
        ],
        "type": "object"
      },
      "tool": "remove_component"
    },
    {
      "description": "Remove components from up to 10,000 entities in one request, with ordered per-item results.",
      "mutates": true,
      "name": "scene.remove_component_many",
      "paramsSchema": {
        "properties": {
          "items": {
            "items": {
              "properties": {
                "id": {
                  "type": "integer"
                },
                "type": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "type"
              ],
              "type": "object"
            },
            "maxItems": 10000,
            "minItems": 1,
            "type": "array"
          }
        },
        "required": [
          "items"
        ],
        "type": "object"
      },
      "tool": "remove_components"
    },
    {
      "description": "Detach a C# script from an entity by type name.",
      "mutates": true,
      "name": "scene.remove_script",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          },
          "type": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "type"
        ],
        "type": "object"
      },
      "tool": "remove_script"
    },
    {
      "description": "Rename an entity by id.",
      "mutates": true,
      "name": "scene.rename",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          },
          "name": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "name"
        ],
        "type": "object"
      },
      "tool": "rename_entity"
    },
    {
      "description": "Discard a prefab instance's component overrides and re-expand it from the prefab. The instance keeps where it is: its root transform is placement, stored on the instance reference rather than as an override, so reverting does not move it back to the origin. 'id' is the instance root. Returns the new root id - the old subtree is destroyed, so an id held from before the call no longer resolves.",
      "mutates": true,
      "name": "scene.revert_prefab_instance",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "tool": "revert_prefab_instance"
    },
    {
      "description": "Save the live scene to its committable .scene.toml. Defaults to the current scene; pass 'name' to save under a different name.",
      "mutates": true,
      "name": "scene.save",
      "paramsSchema": {
        "properties": {
          "name": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "tool": "save_scene"
    },
    {
      "description": "List the C# scripts attached to an entity: each one's type, whether it is attached yet, and its property values in the same shape add_script takes, so a value read here can be written straight back.",
      "mutates": false,
      "name": "scene.scripts",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "tool": "list_scripts"
    },
    {
      "description": "Select entities in the editor. Pass 'id' for a single entity (0 clears the selection), or 'ids' for a multi-selection - the same state a ctrl-click builds, which is what the bulk-edit paths and the Inspector's multi-entity mode act on. The last id given becomes the primary (what the Inspector shows). Ids that name no live entity come back under 'unknown' rather than being dropped silently.",
      "mutates": true,
      "name": "scene.select",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          },
          "ids": {
            "items": {
              "type": "integer"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "tool": "select_entity"
    },
    {
      "description": "The current editor selection: the primary entity id (0 if none) plus all selected ids.",
      "mutates": false,
      "name": "scene.selection",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "get_selection"
    },
    {
      "description": "Set one or more editable fields on a component - the programmatic equivalent of editing it in the Inspector. 'type' is a component name; 'values' is an object mapping field name -> value for ONLY the fields you want to change (partial update; call get_component or list_component_types for field names/types). 'fields' is accepted as a synonym, so get_component output can be posted straight back. Colours/vectors are [x,y,z(,w)] arrays. Returns {id, type, applied:[...]}.",
      "mutates": true,
      "name": "scene.set_component",
      "paramsSchema": {
        "properties": {
          "fields": {
            "type": "object"
          },
          "id": {
            "type": "integer"
          },
          "type": {
            "type": "string"
          },
          "values": {
            "type": "object"
          }
        },
        "required": [
          "id",
          "type"
        ],
        "type": "object"
      },
      "tool": "set_component"
    },
    {
      "description": "Apply reflected component field updates to up to 10,000 entities in one request, with ordered per-item applied-field or error results.",
      "mutates": true,
      "name": "scene.set_component_many",
      "paramsSchema": {
        "properties": {
          "items": {
            "items": {
              "properties": {
                "fields": {
                  "type": "object"
                },
                "id": {
                  "type": "integer"
                },
                "type": {
                  "type": "string"
                },
                "values": {
                  "type": "object"
                }
              },
              "required": [
                "id",
                "type"
              ],
              "type": "object"
            },
            "maxItems": 10000,
            "minItems": 1,
            "type": "array"
          }
        },
        "required": [
          "items"
        ],
        "type": "object"
      },
      "tool": "set_components"
    },
    {
      "description": "Per-component-type entity counts in the live scene - a histogram over the ECS storage pools.",
      "mutates": false,
      "name": "scene.stats",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "scene_stats"
    },
    {
      "description": "Set an entity's transform (position / rotationEuler in degrees / scale) by id. Partial update: whatever you leave out keeps its current value.",
      "mutates": true,
      "name": "scene.transform",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          },
          "position": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "rotationEuler": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          },
          "scale": {
            "items": {
              "type": "number"
            },
            "maxItems": 3,
            "minItems": 3,
            "type": "array"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "tool": "set_transform"
    },
    {
      "description": "Set transforms for up to 10,000 entities in one request. Returns ordered per-item results with partial failures instead of aborting the batch.",
      "mutates": true,
      "name": "scene.transform_many",
      "paramsSchema": {
        "properties": {
          "items": {
            "items": {
              "properties": {
                "id": {
                  "type": "integer"
                },
                "position": {
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "rotationEuler": {
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                },
                "scale": {
                  "items": {
                    "type": "number"
                  },
                  "maxItems": 3,
                  "minItems": 3,
                  "type": "array"
                }
              },
              "required": [
                "id"
              ],
              "type": "object"
            },
            "maxItems": 10000,
            "minItems": 1,
            "type": "array"
          }
        },
        "required": [
          "items"
        ],
        "type": "object"
      },
      "tool": "set_transforms"
    },
    {
      "description": "Break a prefab instance's link: its expanded entities become plain scene entities that no longer track the prefab (and are saved individually). 'id' is the instance root. Returns how many entities were unpacked.",
      "mutates": true,
      "name": "scene.unpack_prefab_instance",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "tool": "unpack_prefab_instance"
    },
    {
      "description": "Read one engine setting by key (e.g. 'window.mode', 'graphics.renderScale') as text, plus whether it is marked as needing a restart. Omit `key` to list every setting.",
      "mutates": false,
      "name": "settings.get",
      "paramsSchema": {
        "properties": {
          "key": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "tool": "get_settings"
    },
    {
      "description": "Append a layer to an entity's tilemap and return its index (paint_tiles' 'layer' parameter).",
      "mutates": true,
      "name": "tiles.add_layer",
      "paramsSchema": {
        "properties": {
          "collision": {
            "type": "boolean"
          },
          "id": {
            "type": "integer"
          },
          "name": {
            "type": "string"
          },
          "opacity": {
            "type": "number"
          },
          "orderInLayer": {
            "type": "integer"
          },
          "save": {
            "type": "boolean"
          },
          "sortingLayer": {
            "type": "integer"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "tool": "add_tile_layer"
    },
    {
      "description": "Create a tileset (every atlas sprite becomes a paintable tile) plus an empty tilemap, and bind the tilemap to an entity's Tile Map component when 'id' is given. 'cellSize' is in world units and must be greater than 0. Returns tileset/tilemap paths and the paintable tile list.",
      "mutates": true,
      "name": "tiles.create",
      "paramsSchema": {
        "properties": {
          "atlas": {
            "type": "string"
          },
          "cellSize": {
            "type": "number"
          },
          "directory": {
            "type": "string"
          },
          "id": {
            "type": "integer"
          },
          "name": {
            "type": "string"
          },
          "solid": {
            "description": "give every tile Full collision (default false)",
            "type": "boolean"
          }
        },
        "required": [
          "atlas"
        ],
        "type": "object"
      },
      "tool": "create_tile_assets"
    },
    {
      "description": "Fill (or erase, when 'tile' is omitted) an inclusive cell rect [x0, y0, x1, y1] on a tilemap layer without sending every cell. Edits join the editor's undo stack. Cells beyond what the physics engine can carry collision for are refused; the error names the limit.",
      "mutates": true,
      "name": "tiles.fill",
      "paramsSchema": {
        "properties": {
          "flipX": {
            "type": "boolean"
          },
          "flipY": {
            "type": "boolean"
          },
          "id": {
            "type": "integer"
          },
          "layer": {
            "type": "integer"
          },
          "rect": {
            "description": "cell rect [x0, y0, x1, y1], inclusive",
            "items": {
              "type": "integer"
            },
            "maxItems": 4,
            "minItems": 4,
            "type": "array"
          },
          "save": {
            "type": "boolean"
          },
          "tile": {
            "type": "integer"
          }
        },
        "required": [
          "id",
          "rect"
        ],
        "type": "object"
      },
      "tool": "fill_tiles"
    },
    {
      "description": "Inspect an entity's tilemap: paintable tiles (tileset indices used by paint_tiles), layers, palette usage, painted-cell bounds, and cell count.",
      "mutates": false,
      "name": "tiles.info",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "tool": "get_tile_map"
    },
    {
      "description": "Paint tilemap cells. Each cell is {x, y, tile?, flipX?, flipY?} where 'tile' is a tileset tile index (get_tile_map lists them); omit 'tile' to erase. Cells beyond what the physics engine can carry collision for are refused, and every cell is checked before any is painted. Edits join the editor's undo stack. 'save' (default true) persists the tilemap asset.",
      "mutates": true,
      "name": "tiles.paint",
      "paramsSchema": {
        "properties": {
          "cells": {
            "items": {
              "properties": {
                "flipX": {
                  "type": "boolean"
                },
                "flipY": {
                  "type": "boolean"
                },
                "tile": {
                  "type": "integer"
                },
                "x": {
                  "type": "integer"
                },
                "y": {
                  "type": "integer"
                }
              },
              "required": [
                "x",
                "y"
              ],
              "type": "object"
            },
            "maxItems": 10000,
            "minItems": 1,
            "type": "array"
          },
          "id": {
            "type": "integer"
          },
          "layer": {
            "type": "integer"
          },
          "save": {
            "type": "boolean"
          }
        },
        "required": [
          "id",
          "cells"
        ],
        "type": "object"
      },
      "tool": "paint_tiles"
    },
    {
      "description": "Read the non-empty cells of an inclusive cell rect [x0, y0, x1, y1] back as {x, y, tile, flipX, flipY} with 'tile' as a tileset index. Large rects are capped - check 'truncated'.",
      "mutates": false,
      "name": "tiles.read",
      "paramsSchema": {
        "properties": {
          "id": {
            "type": "integer"
          },
          "layer": {
            "type": "integer"
          },
          "rect": {
            "description": "cell rect [x0, y0, x1, y1], inclusive",
            "items": {
              "type": "integer"
            },
            "maxItems": 4,
            "minItems": 4,
            "type": "array"
          }
        },
        "required": [
          "id",
          "rect"
        ],
        "type": "object"
      },
      "tool": "read_tiles"
    },
    {
      "description": "Click a widget by {window,label} or a raw {x,y} screen point. Optional 'button' ('left'|'right'), 'double', and 'ctrl'/'shift'/'alt' held for the click (multi-select and range-select). Async: the click plays over a few frames; query/screenshot afterwards to see the effect.",
      "mutates": true,
      "name": "ui.click",
      "paramsSchema": {
        "properties": {
          "alt": {
            "type": "boolean"
          },
          "button": {
            "type": "string"
          },
          "ctrl": {
            "type": "boolean"
          },
          "double": {
            "type": "boolean"
          },
          "label": {
            "type": "string"
          },
          "shift": {
            "type": "boolean"
          },
          "window": {
            "type": "string"
          },
          "x": {
            "type": "number"
          },
          "y": {
            "type": "number"
          }
        },
        "type": "object"
      },
      "tool": "ui_click"
    },
    {
      "description": "Hold the mouse over a widget ({window,label}) or {x,y}. Reveals hover-only UI (tooltips, hover buttons) and sets up a subsequent ui_click on a context-menu item. Held until the next action.",
      "mutates": true,
      "name": "ui.hover",
      "paramsSchema": {
        "properties": {
          "label": {
            "type": "string"
          },
          "window": {
            "type": "string"
          },
          "x": {
            "type": "number"
          },
          "y": {
            "type": "number"
          }
        },
        "type": "object"
      },
      "tool": "ui_hover"
    },
    {
      "description": "Focus a text field ({window,label} or {x,y}) and replace its contents with 'text' (select-all, delete, then type).",
      "mutates": true,
      "name": "ui.input_text",
      "paramsSchema": {
        "properties": {
          "label": {
            "type": "string"
          },
          "text": {
            "type": "string"
          },
          "window": {
            "type": "string"
          },
          "x": {
            "type": "number"
          },
          "y": {
            "type": "number"
          }
        },
        "required": [
          "text"
        ],
        "type": "object"
      },
      "tool": "ui_input_text"
    },
    {
      "description": "Press a key, optionally with modifiers: enter, escape, tab, backspace, delete, space, left, right, up, down, home, end, f1-f12, or any single letter or digit. Set ctrl/shift/alt to send an editor shortcut such as ctrl+z.",
      "mutates": true,
      "name": "ui.key",
      "paramsSchema": {
        "properties": {
          "alt": {
            "type": "boolean"
          },
          "ctrl": {
            "type": "boolean"
          },
          "key": {
            "type": "string"
          },
          "shift": {
            "type": "boolean"
          }
        },
        "required": [
          "key"
        ],
        "type": "object"
      },
      "tool": "ui_key"
    },
    {
      "description": "List on-screen ImGui widgets from the last frame: window, label, screen rect, hovered/active state, and whether the widget was clipped by its window (part of it is not visible). Filter on clippedHorizontally to find controls drawn past the side of a panel; plain clipped also covers rows at the edge of a scrolling list, which is ordinary. Optional 'window'/'label' substring filters. Use this to navigate the UI before ui_click. AN EMPTY LABEL DOES NOT MEAN NO WIDGET: imgui only reports a label for the widget types that volunteer one, so combos and several others come back with a rect and an empty label, as does anything scrolled out of view. Treat a name missing from this list as unknown rather than absent - check the rects, or drive it by {x, y}, which is how an unlabelled combo is clicked.",
      "mutates": false,
      "name": "ui.query",
      "paramsSchema": {
        "properties": {
          "label": {
            "type": "string"
          },
          "window": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "tool": "ui_query"
    },
    {
      "description": "Capture the current editor frame to a .png on disk and return its path - use it to SEE what the editor is rendering. Pass 'path' or get a default under %LOCALAPPDATA%/AetherCore/screenshots.",
      "mutates": false,
      "name": "viewport.screenshot",
      "paramsSchema": {
        "properties": {
          "path": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "tool": "screenshot"
    },
    {
      "description": "Autosaved recovery copies that are NEWER than the scene they shadow, newest first. Empty after a clean save, which is the normal case. Each entry gives the scene name and how far ahead of the saved file it is.",
      "mutates": false,
      "name": "editor.recovery_list",
      "paramsSchema": {
        "properties": null,
        "type": "object"
      },
      "tool": "recovery_list"
    },
    {
      "description": "Promote a scene's autosaved recovery copy over its saved file. Explicit on purpose - nothing is ever restored automatically. Refuses if the copy does not parse, so a stale-but-valid scene is never destroyed by a bad one. Reload the scene afterwards to see it.",
      "mutates": true,
      "name": "editor.recovery_restore",
      "paramsSchema": {
        "properties": {
          "scene": {
            "type": "string"
          }
        },
        "required": [
          "scene"
        ],
        "type": "object"
      },
      "tool": "recovery_restore"
    },
    {
      "description": "Every UI element's RESOLVED rect after the last layout pass - what is actually on screen, not what was authored - with its text, font, size and alignment. For checking alignment, gutters and overlap numerically instead of reading pixels.",
      "mutates": false,
      "name": "ui.layout",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "ui_layout"
    },
    {
      "description": "The scene and prefab NAMES the other methods take: scene.load and scene.save want a scene name, add_prefab_instance wants a prefab's save name. Both are bare names, not paths - assets.list globs the files but leaves the caller stripping a directory and two extensions off each one. 'current' is the scene loaded now.",
      "mutates": false,
      "name": "scene.assets",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "list_scene_assets"
    },
    {
      "description": "Undo the last scene edit, exactly as Ctrl+Z does - selection is remapped through the undone command. The step lands on the next editor frame, so read editor.undo_status afterwards to confirm it was consumed. Refused while playing or compiling, and when there is nothing to undo ('queued' comes back false).",
      "mutates": true,
      "name": "editor.undo",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "editor_undo"
    },
    {
      "description": "Redo the last undone scene edit, exactly as Ctrl+Y does. Like editor.undo, the step lands on the next editor frame and 'queued' comes back false when the redo stack is empty.",
      "mutates": true,
      "name": "editor.redo",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "editor_redo"
    },
    {
      "description": "How deep the editor's undo and redo stacks are, and whether the scene has unsaved edits. Pair it with editor.undo / editor.redo to tell what they will do, and whether one actually consumed an entry.",
      "mutates": false,
      "name": "editor.undo_status",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "undo_status"
    },
    {
      "description": "Ask the application to close, exactly as the title-bar X does. Unsaved work still raises the confirmation prompt rather than being discarded, so check the response's `unsaved` flag and answer the dialog (Save/Discard/Cancel) before expecting the process to exit. A clean editor exits before it can answer at all, so a transport timeout here means it closed - only a reply means it is still up.",
      "mutates": true,
      "name": "app.quit",
      "paramsSchema": {
        "properties": null,
        "type": "object"
      },
      "tool": "quit_app"
    },
    {
      "description": "Set one engine setting by key and apply it live, exactly as the Settings panel does: {key, value} with value as text ('true', '0.5', 'borderless'). Does not save to disk unless {save:true}.",
      "mutates": true,
      "name": "settings.set",
      "paramsSchema": {
        "properties": {
          "key": {
            "type": "string"
          },
          "save": {
            "type": "boolean"
          },
          "value": {
            "type": "string"
          }
        },
        "required": [
          "key",
          "value"
        ],
        "type": "object"
      },
      "tool": "set_setting"
    },
    {
      "description": "Select a project asset by path (project:// or absolute). The File Explorer mirrors it (preview card) and the Inspector shows the asset. Kind is inferred from the extension.",
      "mutates": true,
      "name": "asset.select",
      "paramsSchema": {
        "properties": {
          "path": {
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      },
      "tool": "asset_select"
    },
    {
      "description": "List the C# script type names add_script accepts, as loaded from the project's built assembly. Empty while no assembly is loaded (a project that has not been built yet), which is also when add_script stops checking the name.",
      "mutates": false,
      "name": "scene.script_types",
      "paramsSchema": {
        "properties": {},
        "type": "object"
      },
      "tool": "list_script_types"
    },
    {
      "description": "Press at one widget or point and release at another: {from_window,from_label} or {from_x,from_y} to {to_window,to_label} or {to_x,to_y}. This is how a node-graph link or any drag-and-drop is made. hold_frames keeps the button down at the destination longer, for dwell-triggered behaviour. Async: query or screenshot afterwards.",
      "mutates": true,
      "name": "ui.drag",
      "paramsSchema": {
        "properties": {
          "button": {
            "type": "string"
          },
          "from_label": {
            "type": "string"
          },
          "from_window": {
            "type": "string"
          },
          "from_x": {
            "type": "number"
          },
          "from_y": {
            "type": "number"
          },
          "hold_frames": {
            "type": "number"
          },
          "to_label": {
            "type": "string"
          },
          "to_window": {
            "type": "string"
          },
          "to_x": {
            "type": "number"
          },
          "to_y": {
            "type": "number"
          }
        },
        "type": "object"
      },
      "tool": "ui_drag"
    },
    {
      "description": "Scroll a window's contents so off-screen widgets become visible to ui_query and ui_click. Pass {window} plus 'dy'/'dx' pixel deltas, or 'to' as 'top' or 'bottom'. imgui does not report a label for a widget scrolled out of view, so a control missing from ui_query may simply need scrolling to. Names come from ui_query's 'window' field, child windows included. Returns the resulting scroll and its maximum so a caller can tell when it has reached the end.",
      "mutates": true,
      "name": "ui.scroll",
      "paramsSchema": {
        "properties": {
          "dx": {
            "type": "number"
          },
          "dy": {
            "type": "number"
          },
          "to": {
            "type": "string"
          },
          "window": {
            "type": "string"
          }
        },
        "required": [
          "window"
        ],
        "type": "object"
      },
      "tool": "ui_scroll"
    }
  ]
}